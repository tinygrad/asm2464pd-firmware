name: Build firmware

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y sdcc make python3

      - name: Build firmware
        run: |
          make -C handmade clean
          make -C handmade
          make -C handmade wrapped

      - name: Archive firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: asm2464pd-firmware
          path: |
            handmade/build/firmware.bin
            handmade/build/firmware_wrapped.bin
