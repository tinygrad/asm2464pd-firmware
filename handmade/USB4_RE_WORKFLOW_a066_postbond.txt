{
  "summary": "RE stock a066 INT1 concurrency hang at state-5 + map post-lane-bond router-op flow to PcieTunnel-Enable; diff vs handmade",
  "agentCount": 10,
  "logs": [],
  "result": {
    "immediateBlocker": {
      "rootCause": "The state-5 lane-bond walker parks forever at substate 0x30 because handmade's u4lb_e461() is a no-op stub that never performs the SB-transport push. The 'a066 hang' framing is a misattribution: the walker (u4lb_e672 -> u4lb_walk_8000) runs under EA=0 in the super-loop (main.c:715-734), serialized with a066 -- so a walker that makes no progress presents externally as CPU silence after running once. Ghidra-verified: stock CODE_BANK1::e461 calls e2b9 on BOTH live branches (0x0718==0, and 0x0718!=0 with 0x0776==0). e2b9 writes SB[0x15], runs the d5da SB-transport TX trigger, and sets the in-flight token xdata_00719 = (d5da_result - 1). The host responds to that device->host push by posting its per-lane CL descriptor, which sets xdata_00775. eda0 (CODE_BANK1::eda0) returns its EVAL path (0) ONLY when 0x0775 is set. Handmade u4lb_e461 (usb4_lanebond.h:623-631) returns 1 immediately WITHOUT calling e2b9 and WITHOUT setting 0x0719, so 0x0775 is never driven; u4lb_eda0 (usb4_lanebond.h:615-618) therefore always returns 1 (idle); the LOOP2 0x30 handler's r==0 emit branch (the path that prints Lx:CL0, runs ea7c to program the PHY CC bit2, and advances to 0x50) is never taken; the lane stalls at 0x30 -> SB[0xA0]/[0xA1] never reach CL0=0x02 -> Lane Bonded never fires. NOTE: the a066 da9f (SB teardown) and d5da (unbounded SB[0x2C].2 poll) hangs are RULED OUT on a clean connect -- a066-cfg disassembly of a066 (a13c-a31d) proves that at 0x06ED=5 with no Disconnect/Abr2/BndFail/Disable bits set, ONLY the loop-free CL0/Training/Lane-Bonded reporter branches fire, none reaching da9f/d5da. The genuine unbounded poll (e9e7 -> phy_cc10_cmd_wait bound 0xFFFF) is also NOT reached, because LOOP1 likewise parks at state 0x30 (e461/eda0 break) before ever advancing to LOOP1 state 0x70 where e9e7 lives.",
      "fixFile": "handmade/src/usb4_lanebond.h",
      "fixFunction": "u4lb_e461",
      "fixDescription": "Make u4lb_e461 faithful to stock CODE_BANK1::e461: actually perform the SB-transport push so 0x0719 is set and the host posts its CL descriptor (setting 0x0775), unblocking u4lb_eda0's EVAL path. Replace the stub body (currently: `if (PR(0x0719)!=0) return 0; if (PR(0x0718)==0){ SB_WR(0x04,...); SB_WR(0x04,...);} PR(0x0AAB)=0; return 1;`) with the stock structure: if (PR(0x0719)!=0) return 0 (busy/in-flight); else if (PR(0x0718)==0) { 9960(); e2b9(p1,p2,0); read page1 0x0100; 97e8(0,p1); 97f9(p1); } else if (PR(0x0776)==0) { 9960(); e2b9(p1,p2,PR(0x0718)); } else { 9966(0xAAB); e1cb(p1,p2,PR(0x0776)); }. The load-bearing op is the e2b9(...) call: it writes SB[0x15]=cmd, triggers the bounded d5da TX (u4lb_d5da, already in handmade, with the ++g<0x4000 bound), and sets PR(0x0719) = d5da_result - 1 (the in-flight token, =1 on success). Port e2b9 (and its helpers 9960/997e/9923/9695/99ac/96f7/97ef and e1cb for the read-reply branch) faithfully from the decompiles; keep the d5da SB[0x2C].2 poll BOUNDED as handmade already does (do NOT introduce the stock unbounded poll on the super-loop path). The walker's e461-gated transitions (LOOP2 0x20->0x30 and 0x50->0x60; LOOP1 0x20->0x30, 0x60->0x70, 0xA0->0xA1) advance on the (R7^1) result, which becomes valid once the real token is set. Do NOT touch a066/sb_router.h for the immediate unblock -- the hang is the walker, not the ISR.",
      "confidence": "high",
      "rootCauseConfirmed": "Verified against ghidra: decompile_function(CODE_BANK1::e461) shows e2b9 on both live branches; decompile_function(CODE_BANK1::e2b9) shows `xdata_00719 = cVar2 - 1` after the d5da TX; decompile_function(CODE_BANK1::eda0) shows it returns the EVAL path only when xdata_00775 != 0. Handmade u4lb_e461/u4lb_eda0/u4lb_walk_8000 read directly (usb4_lanebond.h:615-631, 668-763). main.c:715-734 confirms the walker runs under EA=0 (so a walker park is externally indistinguishable from an a066 hang).",
      "evidence": "Stock e461 (ghidra): both live branches call FUN_CODE_BANK1__e2b9(param_1,param_2,...). e2b9 tail: `cVar2 = FUN_CODE_BANK1__d5da(param_1,0); ... xdata_00719 = cVar2 + -1;`. eda0: `cVar1 = xdata_00775; if (cVar1 != 0) { xdata_00775=0; xdata_00719=0; return; }` (this is the EVAL/return-0 path). Handmade u4lb_e461 omits the entire e2b9 call (usb4_lanebond.h:621-631 comment admits 'Full e2b9/e1cb tunnel-poke NOT re-emitted'). Handmade LOOP2 0x30 (usb4_lanebond.h:732-752) only emits CL0 when u4lb_eda0()==0, which can never happen without 0x0775 being set by the host in response to the e2b9 push."
    },
    "nextFixes": [
      {
        "order": 1,
        "title": "Restore the dropped SB[0x64] CL0-ack writeback + per-transition PHY settle; verify a066 W1C acks actually clear",
        "file": "handmade/src/sb_router.h (a066 SB[0x9E].0/.1 branches ~718-732) + handmade/src/usb4_lanebond.h (u4lb_walk_8000 LOOP2)",
        "fn": "sb_router_event_handler SB[0x9E] L0/L1:CL0 branches + u4lb_walk_8000",
        "change": "(a) Re-add the stock SB[0x64] sideband CL0-ack RMW dropped from the L0:CL0/L1:CL0 branches: on SB[0x9E].0 set SB[0x64] bit0 (stock 97a0 read + 97e8 = (v&0xFE)|1); on SB[0x9E].1 set SB[0x64] bit1 (97a0 + 97fc = (v&0xFD)|2) -- cl0-helpers verified this is the ONLY state-changing side effect of those branches and is the device->host CL0 acknowledgement the host's lane-bond confirmation may wait on. (b) Add the per-transition PHY settle phy_cc10_cmd_wait(2,0,0x65) (stock 8501) after the LOOP2 CL0 emit, matching stock 84d6/8345. (c) Audit every a066 W1C ack on SB[0x66]/[0x9E]/[0x26] to confirm write-1-to-clear on the correct SB plane (not an &mask writing 0); a non-clearing W1C leaves C80A.5 level-asserted, re-fires INT1 every IRET, and starves the EA=0 walker -- the documented d4cd &0xF7 bug class.",
        "rationale": "Once e461 unblocks the handshake the host needs the SB[0x64] CL0 ack and a settled PHY to confirm lane-bond; a non-clearing W1C would re-starve the walker. Cheap, stock-faithful, removes the residual a066/walker interaction risk."
      },
      {
        "order": 2,
        "title": "Fix walker PHY-shadow addressing to the stock ROM port-index map instead of raw lane",
        "file": "handmade/src/usb4_lanebond.h",
        "fn": "u4lb_walk_8000 (LOOP1 + LOOP2 0x0800/0x0802/0x081C/0x081E shadow writes)",
        "change": "Index the 0x0800-plane PHY shadow by the stock ROM port index, not raw lane(0/1). Stock 96a7/9803/9805/969e/9887 add ROM[0x21ad]=0x19 / ROM[0x21ae]=0x1c to lane before forming the 0x08xx pointer (96a7->0x0819, 9803->0x081E+lane, 969e->0x081C). e.g. stock LOOP2 0x10 writes 0x081E+lane (|=0x80 then &=0xBF on the SAME byte); handmade writes 0x0802+lane / 0x081E+lane with raw lane (usb4_lanebond.h:723-726,738,741-743,756). Correct the shadow base offsets so lane!=0 hits the same bytes stock does, or the ea7c PHY-CL program targets the wrong shadow byte for L1 and CL0 will not latch on lane 1.",
        "rationale": "Walker agent (impact med): the shadow plane drives the ea7c PHY program the HW uses to bring SB[0xA0]/[0xA1] to CL0. Wrong addressing breaks L1's CL0 emit even after the e461 handshake is fixed."
      },
      {
        "order": 3,
        "title": "Implement the [Pend Int] router-op reply responder (a5d8 -> e1cb/e2b9 -> d5da)",
        "file": "handmade/src/sb_router.h",
        "fn": "sb_router_event_handler SB[0x26].1 branch (~785-790) -- add sb_a5d8_routerop_reply()",
        "change": "Replace the comment-only stub with a faithful a5d8 port: on SB[0x26].1 copy the host inbound router-op descriptor page1 0x0998-0x099B -> 0x0A9D-0x0AA0, gate on 0x0AA0.7 (pending), decode opcode 0x0AA4=(0x0A9F&7): READ -> build reply + send via e1cb; WRITE -> e2b9; both end in the bounded d5da SB[0x15] TX. Descriptor type 8 -> cm_command_dispatch (fix #5). This is the device-side answerer to the host's repeated 0x08/0x09/0x12/0x01/0x05 RX descriptors (the 2a= trace field); without it the host's post-bond config burst gets no device reply. Keep all d5da TX polls bounded; no speculative pokes.",
        "rationale": "routerop agent (impact blocker): the single largest post-bond gap. Required to progress toward PcieTunnel-Enable. NB SB[0x26]=00 on the prior Intel host -- verify it's exercised on the AMD host, but it is the documented stock post-bond responder."
      },
      {
        "order": 4,
        "title": "Implement the PCIe-tunnel power FSM (d855 -> e4ea/e76b/eec7) + its C80A.4 driver (a522/d8d5)",
        "file": "handmade/src/usb4.h",
        "fn": "usb4_int_demux C80A.4 branch (~176) -- add usb4_linkwidth_event_service / tunnel_power_fsm",
        "change": "Replace the `usb4_int_seen|=0x02` C80A.4 stub with the stock chain a522 -> d8d5 -> d855(bitmask): bit4 -> e4ea (clear 0x0B2F, gate C659.0, clear PERST bits0-3 on REG_PCIE_PERST_CTRL/B480, print [PcieTunnel-Enable]); bit2 -> write 4 then e76b (B402/d1c9/eec7, print [PcieTunnel-UPS_Rst_Deassert]); eec7 = [PcieTunnel-Deassert]. These actually clear PERST/B402 and gate C659 to power the tunnel up to the GPU. Port faithfully from the d855/e4ea/e76b/eec7 decompiles; reproduce the exact register writes only, no speculative pokes.",
        "rationale": "routerop agent (impact blocker): the [PcieTunnel-*] transitions that power the tunnel to the GPU never run today. Required for [*** USB4 Gen3 ***] / GPU enumeration in lspci."
      },
      {
        "order": 5,
        "title": "Wire [PcieTunnel-PcieLinkUp] (e911/E763) and cm_command_dispatch (descriptor type 8)",
        "file": "handmade/src/usb4.h (C80A.0-3 branch ~182-187) + handmade/src/sb_router.h (cm_command_dispatch)",
        "fn": "usb4_int_demux C80A.0-3 branch + cm_command_dispatch",
        "change": "(a) Extend the C80A.0-3 E763 handler beyond the bare W1C: on E763.2 print [PcieTunnel-PcieLinkUp] and read the 0x09FA&0x81 route latch (d17e); on E763.3 [PcieLinkDn]. The W1C is already load-bearing; this adds the link-up service body + observability. (b) Implement cm_command_dispatch (the descriptor type-8 sub-handler a5d8 calls in fix #3) to action the host's CM/config command; port from stock, bounded.",
        "rationale": "routerop agent: e911 link-up is a W1C-only stub (med) and cm_command_dispatch is absent (high). Needed to complete tunnel bring-up and answer type-8 router-ops. Last because it depends on fixes #3-#4."
      }
    ],
    "verifyVotes": [
      {
        "refuted": true,
        "reasoning": "I verified the claim against both the ghidra stock decompiles (e461/e2b9/eda0/8000/996d/84fa/cd3f/eaac) and the handmade source (usb4_lanebond.h, sb_router.h, main.c). The fix is REFUTED on efficacy, and the root-cause framing is partly wrong.\n\nWHAT CHECKS OUT (so I'm not dismissing it wholesale): Stock CODE_BANK1::e461 does call e2b9 on both live branches and e1cb on the 0x0776!=0 branch, and e2b9's tail sets xdata_00719 = d5da_result-1; stock eda0 clears 0x0775/0x0719. Handmade u4lb_e461 (usb4_lanebond.h:623-631) is indeed a no-op that omits e2b9 and never sets 0x0719. And within handmade's own model the LOOP2 walker DOES park at substate 0x30: u4lb_eda0() returns 1 (idle) unless 0x0775!=0, and the LOOP2 0x30 emit-CL0 branch (usb4_lanebond.h:732-752) only fires on eda0()==0, so SB[0xA0]/[0xA1] never reach CL0. That internal consistency is real.\n\nWHY THE FIX WOULD NOT WORK (decisive contradiction): The synthesis lead's entire mechanism is \"make e461 do the e2b9 push -> host posts a descriptor -> 0x0775 gets set -> eda0 returns eval-path -> walker advances.\" But in handmade, 0x0775 is set by EXACTLY ONE function: eaac (sb_router.h:111, PR(0x0775)=1), which is reached ONLY via sb_cd3f_dispatch inside sb_connect_present_poll. That whole dispatch is wrapped in `if (PR(0x0777) != 0x0C)` (sb_router.h:381 - a deliberate handmade clobber-gate added as a state-3 fix). At state-5, 0x0777 == 0x0C (it had to be 0x0C for cm_conn_routing_setup to confirm and advance state-3 -> 4 -> 5). So eaac NEVER runs at state-5 and 0x0775 can NEVER be set, regardless of how faithful e461 becomes. The e2b9 push the fix adds has no consumer in the current handmade code; eda0 stays returning 1 and the walker still parks. The fix explicitly says \"Do NOT touch a066/sb_router.h,\" but the real unblock REQUIRES loosening that `0x0777 != 0x0C` gate so eaac can re-relay a host CL descriptor at state-5. The fix targets only u4lb_e461 and is therefore insufficient. (Stock cd3f's gate, verified by decompile, is a complex descriptor-bit condition on 0x0765/0x0752/SB[0x28].4/0x06F0 - NOT 0x0777 - so stock keeps relaying descriptors and re-setting 0x0775 at state-5; that is precisely why stock works and handmade does not. The handmade-specific 0x0777!=0x0C clobber-gate is the real obstacle the proposal overlooks.)\n\nSECONDARY ISSUE - root-cause framing vs the stated symptom: The task symptom is \"the firmware goes SILENT (CPU stall).\" A pure walker park does NOT produce silence: the super-loop `continue`s while 0x06EC!=0 (main.c:810-812) and re-calls u4lb_state5 every iteration, which unconditionally emits triage chars 'Q','d'/'z','R','8','S' (usb4_lanebond.h:896-903). I confirmed NO unbounded loop exists anywhere in the handmade walker or the a066 handler - every poll is bounded (d5da ++g<0x2000/0x4000; phy_cc10_cmd_wait ++g<0xFFFF; a066's only loop is `for idx<4`). So a real CPU stall (true silence) cannot originate in the walker logic; it more plausibly comes from a documented HW read-stall on bad 0x88xx XDATA (usb4_lanebond.h:607-608 warns 0x8814/0x8819 STALL the CPU on read; main.c:701 reads 0x8814; the walker locals/budget live in the XSEG) or the EX1/a066 re-fire path. The proposal's claim that a parked walker \"presents externally as CPU silence\" is contradicted by the unconditional per-iteration UART in the spin path. Also a minor stock-fidelity nit: in stock 8000 the LOOP2 0x30 eval gate is `996d(bVar3)==0` i.e. the 9a11 lane read == 0 (996d merely runs eda0 for side-effects and returns its own arg), not eda0's selector - so handmade's eda0-gated 0x30 model is itself an approximation, further weakening confidence that this single-function fix is the faithful unblock.\n\nBottom line: the proposed e461 fix is necessary-but-not-sufficient and, as scoped (no sb_router.h change), would NOT stop the lane-bond stall, because the 0x0775 producer (eaac) is gated off at state-5 by handmade's 0x0777!=0x0C clobber-gate. Defaulting to refuted is warranted.",
        "alternativeRootCause": "The walker parks at LOOP2 substate 0x30 because u4lb_eda0() can never return its eval-path (0) at state-5: 0x0775 is set ONLY by eaac, and eaac is reached only via sb_cd3f_dispatch in sb_connect_present_poll, which is disabled at state-5 by the handmade-specific `if (PR(0x0777) != 0x0C)` clobber-gate (sb_router.h:381). Stock does NOT gate descriptor relay on 0x0777 (its cd3f gate is a descriptor-bit condition), so stock keeps re-setting 0x0775 at state-5 and the walker advances. Hence the real fix must (a) make e461 faithful AND (b) loosen/replace the 0x0777!=0x0C gate in sb_router.h so eaac can re-relay the host's per-lane CL descriptor at state-5 to set 0x0775 - the proposal's \"do not touch sb_router.h\" scoping makes it insufficient. Separately, the actual CPU-silence symptom is not explained by a walker park (the spin path emits UART every iteration and contains no unbounded loop); silence more likely comes from a HW read-stall on bad 0x88xx XDATA addresses (documented at usb4_lanebond.h:607-608, e.g. main.c:701 reading 0x8814) or the EX1/a066 ISR path, which should be investigated as a distinct fault."
      },
      {
        "refuted": true,
        "reasoning": "The proposal's component facts are mostly verifiable, but its headline causal claim and its \"faithful to stock\" framing are both refuted.\n\nWHAT CHECKS OUT (ghidra + handmade verified):\n- Stock CODE_BANK1::e461 calls e2b9 on both live branches (0x0718==0; and 0x0718!=0 && 0x0776==0). Confirmed by decompile.\n- e2b9 sets the in-flight token: disasm e2f6-e303 shows CLR A; MOV R7,A; LCALL d5da; LCALL 97ef; DEC A; ... MOV DPTR,#0x719; MOVX @DPTR,A, i.e. xdata_00719 = d5da_result - 1. For the staged-descriptor case d5da returns 0 (SB[0x0C]>6 loop path), so 0x0719 becomes 0xFF (nonzero). Confirmed.\n- eda0 returns the EVAL/clear path only when xdata_00775 != 0. Confirmed.\n- Handmade u4lb_e461 (usb4_lanebond.h:623-631) is a stub that omits the e2b9 push and never sets 0x0719; u4lb_eda0 (615-619) returns 1 (idle) until 0x0775 is set; so the handmade walker LOOP1/LOOP2 park at 0x30 (e461 stub ->0x30, then eda0 idle keeps it there). Confirmed.\n- a066 (sb_router_event_handler) at 0x06ED=5 on a clean connect is loop-free; sb_con_consequence (dea1) fast-returns on 0x06EC (sb_router.h:504); so the a066 da9f/d5da hangs are indeed not reached. Consistent with the proposal.\n\nWHY IT IS REFUTED:\n1. SYMPTOM MISMATCH (decisive). The reported symptom is CPU SILENCE / stall. The proposal reframes this as \"a walker that makes no progress presents externally as CPU silence after running once.\" That is false for this code. The super-loop does `if (0x06EC) continue;` (main.c:810-812) while a connect is in progress, so it re-enters the FSM pump every iteration; u4lb_state5 unconditionally prints 'Q', s5_diag prints 'd' or 'z', walk_8000 prints '8', then 'R','S' (all unconditional uart_putc). A parked-but-RETURNING walker therefore emits a continuous REPEATING marker stream (Qd8RS / Qz8RS...), i.e. a chatty livelock, NOT silence. A pure park cannot produce the observed CPU-stall/silence; only an actual blocking loop or interrupt storm or unmapped-MMIO stall can. The proposed root cause does not explain the actual symptom.\n\n2. FIX NOT FAITHFUL TO STOCK'S ACTUAL GATE. Stock's emit/advance gates are the host-supplied SB readout, not eda0/e461 return values: (a) LOOP2 0x30 gates the CL0 emit on 996d(bVar3)==0, and 996d (decompile) returns bVar3 (the masked 9a11 SB lane-descriptor readout) while merely calling eda0() as a side effect to clear 0x0775/0x0719; (b) LOOP1 0x20->0x30 gates on 84fa(bVar3) which (decompile) calls e461() for side-effect and returns bVar3 ^ 1, so the transition fires when the SB readout bVar3==1 — e461's (void) return is discarded. Handmade mismodels BOTH gates as u4lb_eda0()/u4lb_e461() return values. The proposal's fix only drives 0x0719 so handmade's eda0 path clears via 0x0775; it does not restore stock's real readout-driven gate, and handmade's u4lb_e461 would still `return 1` unconditionally, so LOOP1 would advance independent of the host readout — unlike stock. Calling the fix \"faithful to stock e461\" overstates it.\n\n3. SPECULATIVE EFFICACY ON HOST SIDE. Even granting the fix, unblocking requires the host to post a per-lane CL descriptor that sets 0x0775. The project's own HW history (sb_router.h:411-412, 809-812; memory) repeatedly shows the host posting NOTHING (0x2a00 stays zero, SB regs unchanged) — the documented wall. So the fix's effect is unproven and rests on assumed host behavior contradicting prior captures.\n\nNet: the proposal's sub-facts about e461/e2b9/eda0 are correct, but (a) a walker park does not produce the reported silence (the tight super-loop would spew per-iteration markers), so the named root cause does not match the symptom; and (b) the fix is mis-described as faithful — it patches handmade's mis-modeled eda0 gate rather than stock's host-readout gate, and its success depends on unverified host behavior. Default-to-refuted is warranted.",
        "alternativeRootCause": "The CPU silence at 0x06ED=5 is more consistent with an interrupt-driven starvation than a walker park. When the FSM advances to state 5, main.c:762-765 re-enables IE_EX1 (sb_ex1_mask_pending 2->3). The documented C80A.5 connect-storm (sb_router.h:42, 489-499; SB[0x2C].0 is a LEVEL connect the TB4/AMD host HOLDS) then re-fires a066 on every IRET. Although the handmade a066 body is loop-free and sb_con_consequence fast-returns, a level-triggered INT1 that the firmware never deasserts (the host holds connect; handmade only INCs SB[0x2C]) will re-enter the ISR continuously and STARVE the super-loop, presenting as silence — which the project previously fixed for state-3 by self-masking EX1, and which re-opens precisely when EX1 is unmasked at state-5. A second candidate is a stall on an unmapped/HW MMIO read inside a walker callee at lane-state 0x20/0x30 (the project already documents 0x8814/0x8819 reads stalling the CPU). Both should be checked by instrumenting whether the per-iteration walker markers (Q/d/8/R/S) actually REPEAT (park livelock) or STOP after one pass (true stall) — that single observation discriminates the proposal's park theory from an ISR-storm/MMIO-stall, and the proposal did not cite any such repeating-vs-silent UART evidence."
      },
      {
        "refuted": true,
        "reasoning": "I independently verified the synthesis against both the ghidra stock decompiles and the handmade source. Several of its sub-claims are CORRECT, but the proposed fix is insufficient to stop the hang and its root-cause-to-symptom mapping is contradicted, so I refute it.\n\nVERIFIED-CORRECT sub-claims:\n- Stock CODE_BANK1::e461 calls e2b9 on both live branches (0x0718==0; and 0x0718!=0 && 0x0776==0). Confirmed via decompile.\n- Stock e2b9 ends with `xdata_00719 = d5da_result - 1`. Confirmed.\n- Stock eda0 returns the eval-path R7=0 (clearing 0x0775/0x0719) ONLY when 0x0775!=0 (else 2 if 0x0719==2, else 1). Confirmed BYTE-EXACT from the raw disasm (eda0-edbc), and handmade u4lb_eda0 matches it.\n- Stock 8000 LOOP2 0x30 emits CL0 only when eda0 returns 0. I resolved the decompiler artifact: the 0x30 handler calls 996d, and the raw disasm of 996d (996d: LCALL eda0; 9970: MOV 0x22,R7; 9972: MOV A,R7; RET) proves 996d RETURNS eda0's R7 selector (the decompiler's `return param_1` / `DAT_22=param_1` was wrong). So stock IS gated on eda0()==0, and handmade's LOOP2 0x30 (usb4_lanebond.h:732-752) faithfully matches. \n- Handmade u4lb_e461 (usb4_lanebond.h:623-631) indeed omits e2b9 and never sets 0x0719. Confirmed.\n- The walker runs under EA=0 in the super-loop serialized with a066 (main.c:730-751). Confirmed. The handmade a066 itself has NO unbounded loop, and its d4cd (sb_transport_substate_poll, sb_router.h:430) is straight-line — so a066 does not self-hang. That part of the synthesis (a066 ruled out as a self-hanging loop) is plausible.\n\nDECISIVE CONTRADICTIONS (why I refute):\n\n(1) THE PROPOSED FIX CANNOT SET 0x0775, SO IT WOULD NOT UNBLOCK THE WALKER. The synthesis's causal chain is: faithful e461 -> e2b9 device->host push -> \"host posts its CL descriptor, SETTING 0x0775\" -> eda0 eval path fires. But 0x0775 is written to a meaningful value by EXACTLY ONE function: eaac (PR(0x0775)=1). I confirmed via byte-pattern search `90 07 75` that the only three sites touching 0x0775 are eaac (sets =1), eda0 (clears), and b842 (a bulk FSM-reset that clears it alongside 0x0765/0766/0767/06ED). eaac is reached ONLY from cd3f. In stock, cd3f runs from the a066 ISR's d4cd (xrefs: d4df/d502 in sb_transport_substate_poll) on every SB transport edge, so the host's state-5 CL-descriptor response flows host->2a00 RX->d4cd->cd3f->eaac->0x0775=1. In HANDMADE, that path is DEAD at state-5: (a) handmade's ISR d4cd was deliberately stripped of cd3f (sb_router.h:426-429, to dodge the stack cliff), and (b) handmade's super-loop cd3f reproduction (sb_connect_present_poll) is gated OFF once 0x0777==0x0C (sb_router.h:381), which is already true at state-5. So even a byte-faithful e461 would send the push, the host's reply would land in 0x2a00, but nothing in handmade copies it into 0x0775 -> eda0 eval path STILL never fires -> walker still stalls. The fix addresses the device->host half while leaving the host->device consumer (the actual 0x0775 producer, eaac/cd3f) disabled. The fix is necessary-but-not-sufficient; the synthesis's \"would actually stop the hang\" is unsupported.\n\n(2) ROOT CAUSE DOES NOT MATCH THE REPORTED SYMPTOM. The task reports the firmware goes SILENT (CPU stall). But a walker that merely \"parks at 0x30\" is NOT silent: u4lb_e672 calls u4lb_state5 every super-loop iteration whenever the sub-lane bytes are non-zero (which a parked 0x30 keeps true), and u4lb_state5 unconditionally prints 'Q', the s5_diag ('d' + 'z' once budget drains), 'R', the walker's '8', and 'S' EVERY pass. A park therefore produces continuous UART, not silence. The synthesis's reconciliation (\"park is externally indistinguishable from an a066 hang / presents as CPU silence\") is wrong. True silence requires an actual unbounded loop (none exists in the state-5 reachable helpers: u4lb_d5da and phy_cc10_cmd_wait are both bounded — 0x2000/0x4000 and 0xFFFF guards) OR a CPU read-stall on bad XDATA. Notably the code itself documents that addresses 0x8814 and 0x8819 \"STALL the CPU on read\" (usb4_lanebond.h:607-608), and main.c:701-702 still reads XDATA_REG8V(0x8814) as a flag and the s5 budget comment references 0x8814 — a far more plausible silence mechanism than a quiet park, and one the synthesis never considers.\n\nMINOR: the synthesis claims the token is \"=1 on success\"; per d5da's return (cVar5 = SB[0x0C]-7 on the param==0/SB[0x0C]<=6 path), e2b9 sets 0x0719 = SB[0x0C]-8, i.e. a non-1 value in general. The load-bearing fact (token becomes non-zero) is right, but the \"=1\" detail is imprecise.\n\nNet: the synthesis correctly flags the e461 stub as an unfaithful deviation, but it overstates that flag as THE hang and proposes a fix that, by its own causal model, cannot set 0x0775 in handmade (the eaac/cd3f consumer is gated off at state-5), and it cannot explain the reported CPU-silence symptom. Refuted.",
        "alternativeRootCause": "Two stronger candidates the synthesis missed: (A) The 0x0775 PRODUCER is dead at state-5, not just the e461 push. eaac (the sole 0x0775=1 writer) is reached only via cd3f, and handmade disables cd3f at state-5 both in the ISR d4cd (stripped, sb_router.h:426-429) and the super-loop (gated off when 0x0777==0x0C, sb_router.h:381). Stock keeps eaac alive via a066->d4cd->cd3f on every SB transport edge. Any real fix must restore an eaac/cd3f path that runs during state-5 (so host CL-descriptor responses set 0x0775), in addition to making e461 send the push. (B) The reported SILENCE is more consistent with a CPU read-stall than a walker park: the code documents 0x8814/0x8819 as XDATA addresses that stall the CPU on read, yet main.c:701-702 still reads XDATA_REG8V(0x8814); a parked walker would emit continuous Q/d/R/8/S markers (loud), so a true stall points at a bad-XDATA read or an unbounded HW poll, not the e461 logic. Both should be checked against the actual UART trace before accepting the e461-only fix."
      }
    ],
    "survivesRefutation": "0/3 skeptics did NOT refute",
    "hangFindings": [
      {
        "agent": "a066-cfg",
        "summary": "FULL MAP of stock a066 = CODE_BANK1::a066 (the C80A.5 INT1 SB-router handler), verified from decompile + disassemble_bytes of a066 (a0d7-a31d) and every callee. Structure: PART1 (a066-a0d5) loops idx 0..3, services SB[0xC9] connect bits via c3b2 then optionally e52d. PART2 (a0d7-a31d): d4cd transport poll, then a long chain of SB-event W1C branches, each `JNB <bit>, <skip>` so a branch only runs when its HW-set SB bit is present.\\n\\nKEY ANSWER (the hang question): At a CLEAN connect with 0x06ED==5 and per-lane substate 0x20, the ONLY a066 branches that fire are the CL0/Training/Lane-Bonded reporters: SB[0x66].0 (Lane Bonded), SB[0x9E].0/.1 (L0/L1:CL0), SB[0x9E].4/.5 (L0/L1:Training). NONE of these call da9f or d5da. They only do W1C-ack + read SB[0xA0]/[0xA1] low-nibble (9716/971f) + update the 0x074E/0x074F and 0x0819 latches (9874/9868) — all bounded, no polls. So STOCK a066 at clean 0x06ED=5 / lane-state 0x20 contains NO unbounded loop and CANNOT hang.\\n\\nThe da9f-bearing branches (the heavy SB re-init) are ALL gated on FAULT/teardown bits that are NOT set on a clean connect: SB[0x2D].1+SB[0x2C].1 (Disconnect, a139), SB[0x66].2 L0:Abr2 (a243, extra-gated 0x0B1C==0 && !(P1[0x0109]&1)), SB[0x66].5 L1:Abr2 (a26e, same extra gate), SB[0x66].3 L0:Bnd Fail (a285, UNCONDITIONAL da9f), SB[0x66].6 L1:Bnd Fail (a29c, UNCONDITIONAL da9f), SB[0x26].2 L0:Disable (a2be, also d5da first), SB[0x26].4 L1:Disable (a2dd d5da). da9f ITSELF has no loop (linear re-init: sb_block_init + pcie_downstream_link_bringup(1) + E716 PHY reset, then returns), but it TEARS DOWN the whole SB block — taking it on a spurious lane-event bit at 0x06ED=5 would erase lane-bond progress and look like a regression/hang.\\n\\nThe ONLY UNBOUNDED LOOP reachable from a066 is in d5da (the SB-transport TX trigger), at d600-d60b: `R1=#0x2c; LCALL 96e5 (read SB[0x2C]); ANL A,#4; RRC;RRC; ANL A,#0x3f; JZ 0xd600` — a tight busy-spin re-reading SB[0x2C] and looping to d600 FOREVER until SB[0x2C].2 (bit2, TX-done) is set. NO software iteration cap. d5da is reached ONLY via the L0:Disable/L1:Disable branches (SB[0x26].2/.4) — both clean-connect-clear. d4cd->cd3f->af38 also calls d5da in handmade, but stock cd3f path is gated by the 0x0752 descriptor pattern.\\n\\nCONCLUSION for the handmade hang: A faithful stock a066 at clean 0x06ED=5/lane-state 0x20 does NOT hang. If handmade hangs there, the cause is one of: (1) a SPURIOUS SB event bit being read as set (SB[0x26].2/.4 Disable -> d5da unbounded poll on SB[0x2C].2 that never completes, or SB[0x66].3/.6 BndFail -> da9f SB teardown), most likely from handmade's SB_RD page-plane mismapping or a W1C that doesn't actually clear (so the bit re-reads set every INT1 and the level-triggered C80A.5 re-fires); or (2) handmade routing the cd3f/af38->d5da TX path into the ISR where d5da's SB[0x2C].2 poll never satisfies. The stock-faithful fix is to ensure the CL0/Training branches' W1C writes actually clear SB[0x66]/[0x9E]/[0x26] (so no Disable/BndFail bit is spuriously latched) and to bound or remove any d5da SB[0x2C].2 poll on the ISR path.",
        "topCandidates": [
          {
            "rank": 1,
            "candidate": "Spurious SB[0x66].3/.6 (L0/L1:Bnd Fail) bit read as set at 0x06ED=5 -> a285/a29c LCALL da9f (UNCONDITIONAL, no extra gate) -> full SB block teardown/re-init",
            "verdict": "plausible",
            "handmadeLines": "handmade/src/sb_router.h a274-a29c region (sb_router_event_handler SB[0x66] branches, lines ~744-751). In handmade these only W1C + uart_puts (da9f stubbed out as 'faithful arm'), so handmade itself does NOT take da9f here.",
            "evidence": "Stock a285/a29c call da9f UNCONDITIONALLY once SB[0x66].3/.6 is set. da9f re-runs sb_block_init + pcie_downstream_link_bringup(1) + E716 PHY reset, erasing all lane-bond state. If handmade's SB_RD(0x66) mis-reads a set BndFail bit (page/plane mismap, or a W1C that doesn't clear so it re-reads every level-triggered INT1), the device would loop re-initing and never progress. NOTE: handmade currently STUBS da9f on these branches (only W1C+print), so the literal stock hang is masked — but the spurious bit would still cause endless [L0:Bnd Fail] prints + re-entry, starving the super-loop walker. This matches 'runs once then goes silent.'"
          },
          {
            "rank": 2,
            "candidate": "d5da unbounded SB[0x2C].2 poll reached on the ISR path (via L0/L1:Disable SB[0x26].2/.4, OR via the af38 TX path if handmade routes cd3f into the ISR) and SB[0x2C].2 never asserts at 0x06ED=5",
            "verdict": "plausible",
            "handmadeLines": "handmade/src/sb_router.h: d5da repro is INLINED in sb_af38_descriptor_response (lines ~253-260) WITH a bound (++g<0x4000) and W1C SB[0x2C].2. The stock Disable-branch d5da (a2bb/a2dd) is NOT reproduced in handmade (those branches only W1C+print). af38 runs in the SUPER-LOOP not the ISR.",
            "evidence": "d5da d600-d60b is a true unbounded busy-spin (JZ 0xd600) on SB[0x2C].2 with no iteration cap. It is the ONLY unbounded loop reachable from a066. Reached from L0:Disable/L1:Disable (SB[0x26].2/.4) and from af38(cd3f). On the Intel MTL host SB[0x26]=00 (Disable branches not taken) and handmade bounds its af38 d5da poll, so this is currently ruled-down — but if any code path invokes the faithful stock d5da on the ISR with SB[0x2C].2 stuck low at 0x06ED=5, the CPU stalls exactly as described."
          },
          {
            "rank": 3,
            "candidate": "Level-triggered C80A.5 re-fire: a W1C ack (r3_write_dispatch) on SB[0x66]/[0x9E]/[0x26] writing to the wrong plane so the event bit never clears -> INT1 re-fires every IRET -> super-loop (cb10/walker) starves -> apparent CPU stall/silence",
            "verdict": "plausible",
            "handmadeLines": "handmade/src/sb_router.h SB_WR W1C sites: a142 (SB[0x66]=0x01), a1cd (SB[0x9E]=0x02), a229/a24c/a277/a28e (SB[0x66] W1C), a2a5/a2c7 (SB[0x26] W1C). Compare with the documented d4cd fix where &0xF7 wrote 0 to a W1C reg = never cleared (lines ~433-439).",
            "evidence": "The project memory + d4cd comments document a PRIOR bug class where a W1C register was cleared with &mask (writing 0) instead of write-1-to-clear, so the bit stayed set. If any SB[0x66]/[0x9E] CL0/Training W1C at 0x06ED=5 fails to clear, C80A.5 stays asserted (level), the ISR re-runs on every IRET, and the 8000 walker in the super-loop never gets cycles — matching 'walker runs once then firmware goes silent (CPU stall).' This is the SAME deadlock pattern already fixed once for the connect storm (sb_ex1_mask_pending). It is the most likely real cause and is independent of da9f/d5da."
          }
        ]
      },
      {
        "agent": "cl0-helpers",
        "summary": "All 8 stock lane-CL0 servicing helpers called by a066's SB[0x9E].0 (L0:CL0) and SB[0x9E].1 (L1:CL0) branches were decompiled AND disassembled. KEY RESULT: NONE of them contains any loop or HW poll of any kind. Every one is straight-line: a single banked register read or write (tail-call to r3_read_dispatch 0x0bc8 / r3_write_dispatch 0x0be6), or two flat XDATA writes, or a UART hex print. I also disassembled the accessor chain they all tail-call into (0x0bc8 read-dispatch, 0x0be6 write-dispatch, 0x0ac1 banked read, 0x0adc banked write) and confirmed those are ALSO pure straight-line MOVX/MOVC with DPX paging and zero loops/polls. Therefore these 8 helpers CANNOT be the source of the handmade CPU stall at 0x06ED=5 / lane-state 0x20; the hang is elsewhere in a066 (or a callee NOT in this set). \\n\\nLOAD-BEARING ANALYSIS: The SB[0x9E].0/.1 branches are almost entirely diagnostic (read nibble + UART-print 'L0:CL0 '/'L1:CL0 ' + hex). The ONE state-changing side effect the handmade port DROPPED is the SB[0x64] write: stock L0:CL0 does 97a0(read SB[0x64]) then 97e8 = set SB[0x64] bit0; stock L1:CL0 does 97a0 then 97fc = set SB[0x64] bit1. SB[0x64] is a per-link sideband register (bits2:3 also set elsewhere at bank1 95d7 via &0xF3|0x0C). These bit0/bit1 sets are a sideband CL0-reached ACK/status latch written back over the SB plane. However the ACTUAL lane-bond completion is driven by a066 PART1 (SB[0xC9] connect-bitmap -> 0x0109&1 -> sb_lane_bond_complete_tunnel_up/e52d), NOT by these 9E branches — I found NO reader that gates bond completion on SB[0x64].0/.1. So omitting the helpers does NOT prevent the lanes reaching CL0 (that is latched via SB[0xA0]/[0xA1] nibble==2 and the PART1 path); the only risk of dropping the SB[0x64] writeback is that the host-side sideband never sees the device acknowledge CL0 on that register, which COULD stall the host's lane-bond confirmation — but it is not a device-side loop and is not the stall cause. Recommendation: the handmade a066 SB[0x9E].0/.1 simplification is safe w.r.t. hangs; re-adding the SB[0x64] bit0/bit1 writeback is cheap and worth restoring for sideband fidelity, but the CPU stall must be hunted in a different a066 callee.",
        "topCandidates": [
          {
            "candidate": "Dropped SB[0x64] bit0/bit1 sideband CL0-ack writeback (stock 97a0+97e8 / 97a0+97fc)",
            "verdict": "plausible",
            "evidence": "This is the only behavioral delta between stock and handmade in the 9E branches: stock sets SB[0x64].0 (L0:CL0) and SB[0x64].1 (L1:CL0); handmade omits it. SB[0x64] is a per-link sideband register (bits2:3 also set at bank1 95d7). No DEVICE-side reader gates lane-bond on it, so omitting it does NOT cause a device loop, but the host-side sideband may never observe the device's CL0 acknowledgement on SB[0x64], which COULD stall the host's lane-bond confirmation handshake. Worth restoring for fidelity; not the device CPU stall.",
            "handmadeLines": "handmade/src/sb_router.h:718-724 (missing SB_WR(0x64, ...) RMW)"
          },
          {
            "candidate": "The actual a066 CPU stall is in a DIFFERENT callee (not in this helper set)",
            "verdict": "plausible",
            "evidence": "Since all 8 targeted helpers and their accessor chain are loop-free, the stall reported at 0x06ED=5/lane-state 0x20 must originate in another a066 callee: e.g. PART1's sb_channel_connect_service (c3b2) / sb_lane_bond_complete_tunnel_up (e52d-equiv bank1), sb_transport_substate_poll (d4cd), sb_con_consequence (dea1), da9f (disconnect re-init), or eed6/eeee (lane-bonded consequence). Those were out of scope here but are the place to look next for an unbounded HW poll.",
            "handmadeLines": "handmade/src/sb_router.h (PART1 a066-a0d5 and the dea1/da9f/eed6 consequence calls)"
          }
        ]
      },
      {
        "agent": "substate-tunnelup",
        "summary": "ROOT-CAUSE FOUND: the unbounded HW wait that can hang a066/INT1 is inside pcie_downstream_link_bringup (CODE 0x3578), reached from a066 -> e52d (sb_lane_bond_complete_tunnel_up). At 0x3631-0x3639 stock runs an UNBOUNDED, un-timed PCIe-LTSSM poll: `do { A = B455; } while ((A & 2) == 0)` (MOVX B455; ANL #2; RRC; JZ 0x3631). It spins forever until the downstream PCIe link-speed-change/training-done bit (B455.1) sets. There is no sleep, no retry counter, no iteration bound. b7a4 (sb_lane_descriptor_loader) does NOT hang: it has only two BOUNDED ROM-copy loops (CODE 0x213d->XDATA 0x800, 100 iters; CODE 0x21d4->0x07xx, 0x10 iters) plus straight-line SB/engine writes. d4cd (sb_transport_substate_poll) does NOT hang: it does 4 SB reads (0x28/0x2a/0x81/0x83) and on edges calls cd3f; cd3f's heaviest callees (eaac/af38/ebb5) contain only BOUNDED copy loops (0x40 iters, DAT_INTMEM_51<count) — no HW spin.\n\nCRITICAL GATING / WHY HANDMADE HANGS BUT STOCK DOESN'T: e52d only reaches 3578 when XDATA 0x09FA bit1 is set (`if ((xdata_009fa>>1 & 1)!=0)` -> e552 LCALL 0x3578). Inside 3578 the B455 poll is itself further gated: it is only reached if 0x07EF==0 (early-return guard at func top), the cap-read path returns dev-state==4, FUN_CODE_3bcd leaves its flag 0, 0x0AA1!=0, and the 0x05d4-buffer bit2==0 (the `(*ptr>>2 & 1)==0` branch at LAB after 0xb220 cap read). So in STOCK the B455.1 bit is GUARANTEED to set quickly because real downstream PCIe HW (the GPU bridge) actually trains and raises B455.1 — the poll terminates in microseconds. In HANDMADE the same code is being entered from INT1 at 0x06ED=5 while the downstream PCIe side is NOT actually progressing (lane-bond walker only just advanced; B455.1 never sets), so the un-timed `JZ 0x3631` self-loop spins forever = the observed CPU stall / silence.\n\nANSWER TO 'is 3578 reached from a066/INT1 (unsafe) in stock, or deferred?': In STOCK it IS reachable from the same a066 INT1 path (a066 -> e52d -> 3578) — stock does NOT structurally defer it out of INT1; it relies on real HW making B455.1 set fast. The danger is purely that handmade enters this blocking HW poll before the downstream PCIe link can complete. 3578 also contains an OUTER loop `do {...; bank0_da8f_stub_0395();} while (cVar5==0)` (0x3603-0x360d) whose body calls 0395 (a bare DPTR trampoline, NOT a sleep) and a cap-poll 0x051b/0x042b; it is bounded only by that poll returning nonzero — a second potential spin, but the first one (B455.1) is the dominant hang. NOTE: a SEPARATE function at 0xe545 (the func_e4a6 PCIe-tunnel RESET: writes CC31 then `SJMP 0xe55f` self-jump @0xe55f) is NOT in e52d's call chain (xrefs: called from df79 and 059d stub only) — do not conflate it with e52d.\n\nRECOMMENDED FIX DIRECTION: in handmade, either (a) do NOT invoke e52d's pcie_downstream_link_bringup synchronously from INT1 when the downstream link isn't ready (gate on 0x09FA.1 the way stock does AND ensure B455 can actually progress), or (b) add a bounded timeout to the B455.1 wait so INT1 cannot wedge. The handmade port of e52d should replicate stock's 0x09FA.1 gate exactly; if 0x09FA.1 is set in handmade but the GPU bridge HW can't raise B455.1, that bit must not be set yet (premature lane-bond-done latch in 0x0109 page1 driving a066 PART1 -> e52d).",
        "topCandidates": [
          {
            "rank": 1,
            "candidate": "pcie_downstream_link_bringup (CODE:0x3578) B455.1 un-timed poll, reached from a066 -> e52d",
            "verdict": "confirmed",
            "evidence": "Disassembly 0x3631-0x3639: `MOV DPTR,#0xb455; MOVX A,@DPTR; ANL A,#0x2; CLR C; RRC A; JZ 0x3631` = `do{B455}while((B455&2)==0)`, no timeout/sleep/bound. e52d callees list explicitly contains pcie_downstream_link_bringup@CODE:3578 and xref get_xrefs_to(3578) shows `e552 in sb_lane_bond_complete_tunnel_up`. e52d decompile: the 3578 call sits inside `if ((xdata_009fa>>1 & 1)!=0)`. a066 PART1 calls e52d when page1 0x0109.0 set. Stock survives because real GPU PCIe HW raises B455.1 quickly; handmade at 0x06ED=5 has no downstream progress so it spins forever.",
            "handmadeLines": "handmade/src/sb_router.h::sb_router_event_handler (a066 port) -> the e52d/sb_lane_bond_complete_tunnel_up port. Whatever handmade equivalent invokes pcie_downstream_link_bringup / the 0x3578 sequence. Check that the 0x09FA.1 gate is replicated and that page1 0x0109.0 (lane-bond-done) is not latched prematurely; add a bounded timeout to any B455.1 wait."
          },
          {
            "rank": 2,
            "candidate": "pcie_downstream_link_bringup outer loop `do{...;0395();}while(cVar5==0)` (CODE 0x3603-0x360d)",
            "verdict": "plausible",
            "evidence": "0x3606 `MOV A,R7; JZ 0x3625` then 0x3609 `LCALL 0x0395`(bare DPTR trampoline, not a sleep) then 0x360d `JZ 0x35fa` re-loops the cap-poll (0x051b/0x042b). Bounded only by the cap-poll returning nonzero; if the downstream device never responds this also never exits. Secondary to the B455.1 spin which is hit on the success branch (0x3625).",
            "handmadeLines": "same handmade e52d/3578 port path"
          }
        ]
      },
      {
        "agent": "hm-divergence",
        "summary": "KEY DIFF ANALYSIS: why handmade goes silent when a066 (INT1) is invoked at 0x06ED=5 / per-lane substate 0x0759/0x075B=0x20.\n\nARCHITECTURE GROUND TRUTH (from build artifacts handmade/build/firmware.map + firmware.mem + obj/main.rst, obj/crt0.rst):\n- The state-5 walker (u4lb_e672 -> u4lb_state5 -> u4lb_walk_8000) runs INSIDE the super-loop EA=0 critical section: main.c:712 `IE &= ~IE_EA` ... main.c:731 `IE |= IE_EA`. So a066/INT1 CANNOT preempt the walker; the two deep call paths are never simultaneously on the stack. After the walker returns and EA is restored, main.c:790 `if(0x06EC) continue;` re-enters the FSM pump; in the brief EA=1 window a066 DOES fire because C80A.5 is level-asserted at state-5 (SB[0x2C].0=1 connect held — confirmed by the stock trace S=...F1... bytes). So \"a066 runs at state-5\" is real, but SERIALLY with the walker, not nested.\n- crt0 sets sp=0x6B (crt0.rst:57). DSEG=0x00-0x6C (108B). Stack = 0x6C..0xFF = 148B, NON-overlapping (the IRAM-headroom fix relocated all persistent globals to XDATA 0x88xx). Deepest a066 frame depth ~7, fits.\n- The dangerous READ-STALL XDATA window: build proves u4lb_s5_print_budget=0x882B (XSEG s_XSEG=0x8820, safe), edf5 budget=0x8818, eaac/af38 budgets=0x8815/16/17. NONE land in the documented 0x8814 / 0x8819-0x881F read-stall window. The only access to 0x8814 is main.c:698/699 (`mov dptr,#0x8814`), gated on sb_ex1_mask_pending==2 — which is 3 by state-5 (set once at main.c:743 when 0x06ED leaves 3, never reset to 2). So no stall-window read fires at state-5.\n\nCONCLUSION: candidate (a) stall-window read and (c) stack overflow are RULED OUT by the build artifacts. The remaining plausible hangs are (1) the unbounded 0xFFFF phy_cc10_cmd_wait poll reachable from the walker (u4lb_e9e7, NOT a066 — the silence is the WALKER hanging under EA=0, which is indistinguishable from \"a066 hung\" externally), and (2) a066 PART1 sb_channel_connect_service taking the n==3 link-reset / n==0 da9f branch because sb_lane_bonded_consequence re-armed SB[0xC9]=0xFF.\n\nNOTE THE FRAMING TRAP: because the walker runs with EA=0, a hang inside the walker (or any callee it invokes) presents externally as total CPU silence with no a066 progress — easily MISATTRIBUTED to \"a066 hangs\". The evidence points more at a walker-side unbounded poll than at a066 itself.",
        "topCandidates": [
          {
            "rank": 1,
            "candidate": "(b-variant) Walker-side unbounded/huge-bound PHY poll: u4lb_e9e7 -> phy_cc10_cmd_wait polls CC11.1 with bound 0xFFFF (boot_phy.h:40), reachable from u4lb_walk_8000 LOOP1 s==0x70 gated on PR(0x0AB3). Runs under EA=0, so a stall there looks externally identical to 'a066 hangs / CPU silent'.",
            "verdict": "plausible",
            "handmadeLines": "usb4_lanebond.h:696 (if(PR(0x0AB3)) u4lb_e9e7()), usb4_lanebond.h:373-382 (u4lb_e9e7 body, 2x phy_cc10_cmd_wait + ebde 2x), boot_phy.h:40 (while(!CC11.1 && ++g<0xFFFF)), boot_phy.h:390-391 (ebde C2D0.5/C350.5 spins bound 0x2000). Walker runs EA=0: main.c:712,731.",
            "evidence": "phy_cc10_cmd_wait bound is 0xFFFF=65535 (NOT a tight bound). At state-5 the deep tunnel-power / E764-train nodes are ROUND-B PLACEHOLDERS (usb4_lanebond.h:525-531, b0b4; cm_conn_routing c586/e175 OMITTED at usb4_lanebond.h:282-284), so the PHY is NOT in the state stock leaves it — CC11.1 may never ack, burning the full 65535-iter poll 4x under EA=0 = visible silence. This is the candidate that best matches 'runs once then goes silent', and the EA=0 framing makes it MASQUERADE as an a066 hang. Note: in STOCK, e9e7 is gated on 0x0AB3 too and runs fine because the PHY ack arrives — handmade's placeholder PHY state is the divergence."
          },
          {
            "rank": 2,
            "candidate": "(d) a066 PART1 sb_channel_connect_service fires at state-5 because sb_lane_bonded_consequence re-armed SB[0xC9]=0xFF, and takes the n==3 link-reset (SB[0x15]=0x83) or n==0 da9f-special branch on the host's connect descriptor.",
            "verdict": "plausible",
            "handmadeLines": "sb_router.h:709-714 (sb_lane_bonded_consequence on SB[0x66].0) -> sb.h-side SB_WR(0xC9,0xFF) at sb_router.h:576; sb_router.h:668-678 (PART1 fires if SB[0xC9]&(1<<(4+idx)) && 0x06F1==idx); sb_router.h:601-655 (sb_channel_connect_service n-dispatch); sb_router.h:623-630 (n==3), sb_router.h:632-654 (n==0 da9f).",
            "evidence": "The re-armed SB[0xC9]=0xFF means PART1 CAN dispatch at state-5. BUT both heavy branches (n==3 c4a0 LJMP da9f, n==0 c451 da9f) are intentionally STUBBED to no-op arms in handmade (comments sb_router.h:626-630, 644). So they cannot WEDGE — they W1C and return. This is RULED-OUT as a *hang* but is a real *behavioral divergence* (stock would re-init SB; handmade silently no-ops, possibly mis-acking SB[0xC9] connect bits and corrupting the per-port round-robin 0x06F1 a066 advances at sb_router.h:675). Demoted from hang to correctness risk."
          }
        ]
      }
    ],
    "postbondSummary": [
      {
        "agent": "walker",
        "summary": "RE'd the stock state-5 CL walker (CODE_BANK1::8000 + the 0x075b-plane LOOP2) and compared it to handmade/src/usb4_lanebond.h. STRUCTURE OF STOCK 8000: a single per-lane loop (DAT_INTMEM_21=lane 0..1) runs TWO sequential dispatch passes — pass1 on substate byte 0x0759+lane (jump-table via 0def, states 10/20/30/40/50/60/70/80/90/A0/A1) and pass2 on 0x075b+lane (inline CJNE ladder, states 10/20/30/50/60). 850b is the 0x0718!=4 alternate (DEAD on the live AMD path, which is 0x0718==4 -> 8000). The CL0 emit + Lane-Bond is driven ENTIRELY by pass2 (0x075b): in state 0x30, after eda0 returns 0 (the EVAL path), the handler reads the host snapshot 0x0779+lane via 982b; if snap.4 set -> reset to 0; elif snap.7 CLEAR -> back to 0x20; else (snap.7 SET) it latches v=snap&0x0F into 0x0761+lane, programs the 0x081E/0x0802 PHY shadow, prints \\\"Lx:CL0\\\" (via FUN_CODE_0c7a), calls ea7c(v,lane) to set the PHY CC bit2 (C2CB/C34B), and advances to 0x50. The walker NEVER writes SB[0xA0]/[0xA1] directly — SB[0xA0/A1]->CL0=0x02 is the HW/host response to the PHY program once both lanes reach the 0x30->0x50 emit. EXIT/finalize: e672 fires eb62(0,0) when 0x0759/0x075a/0x075b/0x075c all reach 0. KEY FINDING: handmade's LOOP2 state ladder (0x075b: 10/20/30/50/60) is a FAITHFUL transcription of the stock pass2 transitions, BUT every state transition is gated by two helpers — e461() (the per-state push) and eda0() (the response poll) — and the handmade e461 is a NO-OP STUB that omits the load-bearing e2b9 SB-transport send. In stock, e461 (live path 0x0718!=0 && 0x0776==0) calls e2b9(...) which writes SB[0x15]=cmd + triggers d5da TX and SETS the in-flight token 0x0719=1; the host then posts its CL descriptor which sets 0x0775; eda0 sees 0x0775!=0 and returns 0 (the EVAL path) that lets state 0x30 emit CL0. Handmade e461 returns 1 immediately WITHOUT sending e2b9 and WITHOUT setting 0x0719, so 0x0775 is never driven, eda0 always returns 1 (IDLE), and the lane PARKS at state 0x30 forever, never emitting CL0=0x02 / never reaching Lane Bonded. This is the walker's blocking divergence (independent of, and in addition to, the a066/INT1 hang noted in the task). Secondary divergences: handmade LOOP1 (0x0759 FSM) is largely fabricated vs the stock 0def-table handlers; the 0x0800/0x0802/0x081C/0x081E PHY shadow is indexed by raw lane(0/1) instead of the stock ROM port-index map (ROM 0x21ad=0x19, 0x21ae=0x1c -> 96a7 reads 0x0819/0x081C); and the per-transition phy settle 8501 (=phy_cc10_cmd_wait(2,0,0x65)) reached via 84d6 is omitted. File: /home/batman/asm2464pd-firmware/handmade/src/usb4_lanebond.h.</summary>\n<divergences>\n<divergence>\n<what>e461() is a no-op stub that omits the load-bearing e2b9 SB-transport send (and never sets the 0x0719 in-flight token). In stock, e461 on the live path (0x0718!=0 && 0x0776==0) does 9960() then e2b9(R7,R5,0x0718) which writes SB[0x15]=cmd, triggers the d5da TX, and sets xdata 0x0719 = (d5da result - 1) = 1. That device->host transport message is what elicits the host to post its per-lane CL descriptor, which sets 0x0775. Without the e2b9 send, 0x0775 is never driven by the host, so eda0 (below) can never take its EVAL path, so NO lane ever emits CL0 and SB[0xA0]/[0xA1] never reach 0x02.</what>\n<handmade>static uint8_t u4lb_e461(void){ if(PR(0x0719)!=0) return 0; if(PR(0x0718)==0){ SB_WR(0x04,...); SB_WR(0x04,...);} PR(0x0AAB)=0; return 1; }</handmade>\n<handmadeLines>623-631</handmadeLines>\n<stockBehavior>CODE_BANK1::e461: if(0x0719!=0)return; if(0x0718==0){9960();e2b9(p1,p2,0);sb_read_page1_0100();97e8();97f9();} else if(0x0776==0){9960();e2b9(p1,p2,0x0718);} else {9966(0xaab);e1cb(p1,p2,0x0776);}. e2b9 sets SB[0x15], runs d5da TX trigger, and writes xdata_0x0719 = d5da_result-1 (=1, the in-flight token).</stockBehavior>\n<impact>blocker</impact>\n</divergence>\n<divergence>\n<what>Because e461 never sets 0x0719 and the host never sets 0x0775, eda0() (called as the gate in LOOP2 states 0x30 and 0x60) ALWAYS returns 1 (IDLE), never 0 (EVAL). In the handmade 0x30 handler, the r==0 branch (which reads 0x0779+lane snap.7 and emits CL0 -> ea7c -> state 0x50) and the r==2 branch are therefore NEVER taken: the lane parks at substate 0x30 indefinitely. No \\\"Lx:CL0\\\" print, no ea7c PHY-CL program, no advance to 0x50 -> Lane Bonded never triggers. (Symptom-consistent with the walker running once then making no further progress.)</what>\n<handmade>LOOP2 state 0x30: r=u4lb_eda0(); if(r==0){...emit CL0, ea7c(v,lane), state=0x50} else if(r==2) state=0x20; /* r==1 (idle) falls through: state stays 0x30 forever */</handmade>\n<handmadeLines>729-749</handmadeLines>\n<stockBehavior>Stock state 0x30 (83ce): 996d()=eda0; JZ(r==0)-> read 0x0779+lane via 982b; snap.4 set->state=0; snap.7 clear->state=0x20; snap.7 set->latch v, write 0x081E/0x0802 shadow, FUN_CODE_0c7a print \\\"Lx:CL0\\\", ea7c(v,lane), state=0x50. eda0 returns 0 only because the host (triggered by e461's e2b9) set 0x0775.</stockBehavior>\n<impact>blocker</impact>\n</divergence>\n<divergence>\n<what>The per-transition PHY settle 8501 is omitted from the handmade walker. After several LOOP2 transitions (the 84d6 -> LCALL 8501 path, and the LOOP1 8345 -> LCALL 8501 path) the stock walker issues phy_cc10_cmd_wait(2,0,0x65) (8501 = R5=0x65,R4=0,R7=2,LCALL 0x051b). The handmade walker has no equivalent call after any state transition, so the PHY/CC-orientation programming done by ea7c is never committed/settled the way stock does between lane steps.</what>\n<handmade>No call to a phy_cc10_cmd_wait(2,0,0x65) equivalent anywhere in u4lb_walk_8000 / its LOOP2 transitions.</handmade>\n<handmadeLines>719-759</handmadeLines>\n<stockBehavior>8501: MOV R5,#0x65; MOV R4,#0; MOV R7,#2; LCALL 0x051b (phy_cc10_cmd_wait(2,0,0x65)); reached after LOOP2 emit/transition via 84d6 and after LOOP1 transitions via 8345.</stockBehavior>\n<impact>med</impact>\n</divergence>\n<divergence>\n<what>The 0x0800/0x0802/0x081C/0x081E PHY-shadow planes are indexed by the RAW lane (0/1) in handmade, but the stock walker indexes them by a ROM-mapped PORT index. 96a7/9803/9805/969e/9887 all add a ROM table value (ROM 0x21ad=0x19, ROM 0x21ae=0x1c) to lane before forming the 0x08xx pointer: 96a7(ROM[0x21ad]=0x19) -> 0x0819, 9803 -> 0x0800+2+0x1c+lane = 0x081E+lane, 969e uses 0x1c -> 0x081C. So e.g. the stock LOOP2 0x10 handler writes 0x081E+lane (|=0x80 then &=0xBF), not 0x0802+lane. Handmade writes 0x0802+lane / 0x081E+lane / 0x0800+lane / 0x081C+lane with raw lane, hitting different bytes of the PHY shadow than stock for lane!=0 and for the first 0x10 write.</what>\n<handmade>e.g. LOOP2 0x10: PR(0x0802+lane)|=0x80; PR(0x081E+lane)&=0xBF; and throughout: PR(0x0800+lane), PR(0x0802+lane), PR(0x081C+lane), PR(0x081E+lane) all use raw lane.</handmade>\n<handmadeLines>723-726,673-675,702-704,724,738,756</handmadeLines>\n<stockBehavior>Stock uses 96a7/9803/9805/969e/9887 = 0x0800 + (ROM_port_idx + lane). ROM[0x21ad]=0x19, ROM[0x21ae]=0x1c. 96a7(0x19)=0x0819; 9803(idx)=0x0800+2+idx; LOOP2 0x10 (83a5) writes 0x081E+lane both times (|=0x80 then &=0xBF on the SAME 0x081E byte).</stockBehavior>\n<impact>med</impact>\n</divergence>\n<divergence>\n<what>The handmade LOOP1 FSM (substate 0x0759+lane, states 10/20/30/40/50/60/70/80/90/A0/A1) is a fabricated if/else ladder that does not match the stock 0def jump-table handlers. The stock 0x30 handler (807a) does 996d()/985b()/99d1()/97d5() with different reads and writes; handmade's 0x30 reads 0x077B+lane (stock LOOP1 0x30 uses 985b=0x077B but via a different code path / different transitions). LOOP1 substates 0x0759/0x075a must reach 0 for e672's finalize (eb62(0,0)); a mis-modeled LOOP1 can either stall those bytes (finalize never fires) or zero them prematurely (finalize fires before LOOP2 emits CL0). On the live path this is secondary to the e461/eda0 blocker but will need a faithful pass1 transcription once the handshake is fixed.</what>\n<handmade>u4lb_walk_8000 LOOP1: hand-written if/else on s=PR(0x0759+lane) with states 0x10..0xA1, reading 0x077B+lane / e461 / eda0 in a ladder not derived from the 0def table handlers.</handmade>\n<handmadeLines>668-718</handmadeLines>\n<stockBehavior>Stock pass1: 96d7 -> read 0x0759+lane -> LCALL 0def jump table (802a: 10->804f,20->8069,30->807a,40->80ca,50->80d7,60->8251,70->8262,80->82d5,90->82fa,A0->831a,A1->8327, default 8355). Handler bodies use 969e/96a9/96a7/985b/99d1/97d5/9887/9803/9805 with ROM-port indexing, distinct from the handmade ladder.</stockBehavior>\n<impact>high</impact>\n</divergence>\n<divergence>\n<what>e461's return value is approximated. Stock e461 is void; the walker reads success via 84fa/84f3 = (e461;R7)^1, where R7 after e461 carries the e2b9/97f9 side-effect (the 0x0719 token, normally 1 -> 84fa returns 0 -> advance). Handmade returns 1 unconditionally after the 0x0719!=0 guard. This happens to approximate the 'advance' decision but, combined with the missing e2b9, makes every e461-gated transition (0x20->0x30, 0x50->0x60 in LOOP2; 0x20/0x60/0xA0 in LOOP1) advance WITHOUT actually performing the SB-transport push the next state's eda0 is waiting on.</what>\n<handmade>u4lb_e461 returns 1 unconditionally (after the 0x0719 guard); callers test u4lb_e461()==1 to advance.</handmade>\n<handmadeLines>623-631,678,690,710,728,751</handmadeLines>\n<stockBehavior>84fa: LCALL e461; A=R7; XRL #1; RET -> returns (R7 from e461)^1. R7 is left by e2b9's tail (d5da result -> 0x0719). Advance happens because the real push set the token to 1.</stockBehavior>\n<impact>high</impact>\n</divergence>\n</divergences>\n<steps>\n<step>\n<order>1</order>\n<step>e672 dispatch: 0x06ED==5 -> if all of 0x0759/0x075a/0x075b/0x075c==0 eb62(0,0) finalize; elif 0x0718==4 -> 8000 else 850b</step>\n<fn>CODE_BANK1::e672</fn>\n<registers>0x06ED, 0x0718, 0x0759/0x075a/0x075b/0x075c</registers>\n<implementedInHandmade>true</implementedInHandmade>\n<notes>Faithful (u4lb_e672 lines 909-928).</notes>\n</step>\n<step>\n<order>2</order>\n<step>8000 single per-lane loop, pass1 dispatches on 0x0759+lane via 0def jump table</step>\n<fn>CODE_BANK1::8000 (pass1, 0def table @802a)</fn>\n<registers>0x0759+lane substate; 0x0800/0x081C/0x0819 shadow (ROM-port indexed)</registers>\n<implementedInHandmade>true</implementedInHandmade>\n<notes>Implemented but as a fabricated if/else ladder, not the stock table handlers; ROM port-index addressing not reproduced.</notes>\n</step>\n<step>\n<order>3</order>\n<step>8000 pass2 dispatches on 0x075b+lane via inline CJNE ladder (states 10/20/30/50/60)</step>\n<fn>CODE_BANK1::8000 (pass2 @8384)</fn>\n<registers>0x075b+lane substate</registers>\n<implementedInHandmade>true</implementedInHandmade>\n<notes>State ladder faithfully matches stock transitions.</notes>\n</step>\n<step>\n<order>4</order>\n<step>Each e461-gated transition (0x20->0x30, 0x50->0x60) must SEND e2b9 SB-transport push and set 0x0719=1</step>\n<fn>CODE_BANK1::e461 -> e2b9 -> d5da/97ef</fn>\n<registers>0x0719 (in-flight token), SB[0x15], SB[0x10], SB[0x2C]</registers>\n<implementedInHandmade>false</implementedInHandmade>\n<notes>BLOCKER: handmade e461 is a no-op stub, never sends e2b9, never sets 0x0719.</notes>\n</step>\n<step>\n<order>5</order>\n<step>Host responds to the e2b9 push by posting the per-lane CL descriptor -> sets 0x0775</step>\n<fn>(host/HW; consumed by eda0)</fn>\n<registers>0x0775, 0x0779+lane snapshot</registers>\n<implementedInHandmade>false</implementedInHandmade>\n<notes>Cannot happen because step 4 never sends the push.</notes>\n</step>\n<step>\n<order>6</order>\n<step>State 0x30: eda0 returns 0 (because 0x0775 set) -> read 0x0779+lane; snap.7 set -> emit CL0</step>\n<fn>CODE_BANK1::996d(eda0), 982b(read 0x0779+lane)</fn>\n<registers>0x0775, 0x0719, 0x0779+lane, 0x0761+lane</registers>\n<implementedInHandmade>true</implementedInHandmade>\n<notes>Logic present and correct, but unreachable: eda0 always returns 1 (idle) so the r==0 emit branch is never taken.</notes>\n</step>\n<step>\n<order>7</order>\n<step>CL0 emit: latch v=snap&0x0F, program 0x081E/0x0802 shadow, print Lx:CL0 (0c7a), ea7c(v,lane) sets PHY CC bit2 C2CB/C34B, state=0x50</step>\n<fn>FUN_CODE_0c7a, CODE_BANK1::ea7c</fn>\n<registers>0x0761+lane, 0x081E/0x0802 shadow, C2CB/C34B, 0x075b+lane</registers>\n<implementedInHandmade>true</implementedInHandmade>\n<notes>ea7c port is faithful (C6DB orientation + bit2). Never executed due to step 4/5/6 break. Shadow plane uses raw lane not ROM index.</notes>\n</step>\n<step>\n<order>8</order>\n<step>HW/host drives SB[0xA0]/[0xA1] 01->02 (CL0) in response to the PHY program; both lanes at CL0 -> Lane Bonded -> PcieLinkUp</step>\n<fn>(HW; downstream of ea7c, not a firmware write)</fn>\n<registers>SB[0xA0], SB[0xA1]</registers>\n<implementedInHandmade>false</implementedInHandmade>\n<notes>The walker never writes SB[0xA0]/[0xA1] directly; this is the HW success response, gated on steps 4-7 actually running. Confirms the success axis is the 0x30->0x50 emit for both lanes, not a direct SB[0xA0/A1] write.</notes>\n</step>\n<step>\n<order>9</order>\n<step>Per-transition PHY settle phy_cc10_cmd_wait(2,0,0x65) after emit/transition (8501 via 84d6/8345)</step>\n<fn>CODE_BANK1::8501</fn>\n<registers>CC10 mailbox, C8B0</registers>\n<implementedInHandmade>false</implementedInHandmade>\n<notes>Omitted from the handmade walker entirely.</notes>\n</step>\n</steps>\n</invoke>\n"
      },
      {
        "agent": "routerop",
        "summary": "Mapped the stock post-lane-bond host-config phase (between \"Lane Bonded\"/\"L0:CL0 02\" and \"[PcieTunnel-Enable]\"/\"[*** USB4 Gen3 ***]\") from fw_tinygrad.bin. The phase is driven by TWO concurrent device-side engines, both reached from the a066 SB-router INT1 handler and the C80A.0-3/C80A.4 INT1 sources:\n\n(1) THE [Pend Int] ROUTER-OP RESPONDER (the long descriptor TX sequence). a066 @a15c reads SB[0x26].1 and calls CODE_BANK1::a5d8. a5d8 copies the host's inbound router-op descriptor from page1 0x0998-0x099B into 0x0A9D-0x0AA0, gates on 0x0AA0.7 (pending), decodes the opcode field 0x0AA4=(0x0A9F&7): for a READ it builds the reply and sends via CODE_BANK1::e1cb; for a WRITE via CODE_BANK1::e2b9; both call FUN_CODE_BANK1__d5da (the SB[0x15] TX trigger). For descriptor type 8 it calls cm_command_dispatch. e1cb/e2b9 write SB[0x15] (via 972a/96f7) and drive d5da. THIS is the device-side answerer to the host's 0x08/0x09/0x12/0x01/0x05 RX descriptors (the 2a= field in the trace). The descriptor TYPE dispatch is cd3f (already in handmade) for the SB-transport connect plane; the per-[Pend Int] reply is a5d8.\n\n(2) THE PCIe-TUNNEL POWER STATE MACHINE (the [PcieTunnel-*] markers). The suffix-printer is tunnel_print_prefix_ef03 (uart_puts \"[PcieTunnel-\") + FUN_CODE_538d(suffix). I decoded each suffix from the mov R2:R1 immediate before the second 538d: [PcieTunnel-Deassert](0x37EC)=bank1_eec7; [PcieTunnel-Enable](0x37F6)=e4ea; [PcieTunnel-UPS_Rst_Deassert](0x3829)=e76b; [PcieTunnel-PcieLinkUp](0x38B4)=tunnel_link_event_e763 @e911. The master FSM is bank1_d855 (reached via stub 0548 from d8d5, itself from usb4_linkwidth_event_service_a522 = the C80A.4 source): d855 dispatches on a bitmask param: bit4 -> e4ea (clear C659, PERST clears in e4ea, print \"Enable]\"); bit2 -> writes 4 then e76b (B402/d1c9/eec7, print \"UPS_Rst_Deassert]\"); bit3 -> ee29 + DisPath/Sleep; bit1 -> ee94 + B402 + \"UPS_Rst_Assert]\". eec7 (\"Deassert]\") = bank0_e8d9_stub_0476 + print, called inside e4ea/e76b. [PcieTunnel-PcieLinkUp] is e911 (C80A.0-3 source, E763.2 W1C bit2, then d17e route-latch read). LANE BONDED is a066 SB[0x66].0 -> eed6 -> (when P1[0x0109].1) e52d=sb_lane_bond_complete_tunnel_up which loads DROM (b7a4), calls eb0a (\"Sleep]\"), and if 0x09FA.1 runs pcie_downstream_link_bringup(0x0AEF). router-op RX is enabled by e56f (EC00|=1, EA88=100/EA89=0x24, C807 bit7 RX-enable).\n\nNOTE / TASK CORRECTION: a327 and the chain 180d/3cb8/38d4/425f/2bea are NOT the SB router-op responder — they are the USB-mass-storage SCSI/CBW-CSW transport (REG_USB_CBW/CSW, REG_NVME_*, sector counts). a327 is referenced only by USB-MSC functions (d4c8, c3ce, cbf8, ccb3), not by the a066/SB path. The genuine post-bond device->host SB reply path is a066 -> a5d8 -> e1cb/e2b9 -> d5da, plus the tunnel power FSM d855/e4ea/e76b/eec7 and the e911 link-up handler.\n\nHANDMADE STATUS: the C80A.0-3 path (usb4.h usb4_int_demux lines 182-187) only W1C-acks E763 (no [PcieLinkUp] print, no d17e) - functionally the W1C is present but the tunnel-link-up service body is a stub. The [Pend Int] a5d8 responder (sb_router.h lines 785-790) is a COMMENT-ONLY stub: it W1Cs SB[0x26].1 and prints [Pend Int] but does NOT build/send the e1cb/e2b9/d5da reply. The tunnel power FSM (d855/e4ea/e76b/eec7 -> [Deassert]/[Enable]/[UPS_Rst_Deassert]) and the C80A.4 linkwidth service a522/d8d5 are NOT implemented (usb4.h line 176 only sets usb4_int_seen|=0x02). cm_command_dispatch (type 8) not implemented. The lane-bond-complete tunnel-up (e52d) IS partly wired via sb_tunnel_up_pending -> pcie_power_on (sb_router.h e52d / main.c lines 774-786), gated on both lanes CL0. e56f router-op RX-enable IS implemented (usb4_irq.h usb4_routerop_init). cm_routerop_mailbox (c0a5, EC06.0) IS wired (usb4.h) but its deep reply bodies are stubbed (host never posts on this HOST so it is a NO-OP)."
      }
    ]
  }
}